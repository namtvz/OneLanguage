- if channels.empty?
  %h4
    You didn't join any channel yet
- else
  %table.table.table-striped
    %thead
      %tr
        %th
          Channel
        %th
          Owner
        %th
          Partner
        %th
          Start Time
        %th
          Length
    %tbody
      - channels.each do |channel|
        = render 'channel_row', channel: channel, type: :translator
  = paginate channels